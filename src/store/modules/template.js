import { useApi } from '@/composables/useApi';

const api = useApi();

const state = () => {};

const getters = {};

const mutations = {};

const actions = {};

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
};
